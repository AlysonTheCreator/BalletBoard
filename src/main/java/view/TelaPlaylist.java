package view;

import javax.swing.*;

public class TelaPlaylist extends JFrame {
    public TelaPlaylist(){
        setTitle("Tela Playlist");
        setSize(700,900);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setResizable(false);
        setLocationRelativeTo(null);

        setVisible(true);
    }
}
